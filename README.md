# Telegram Bot для регистрации курьеров

Бот для регистрации курьеров с отправкой уведомлений в канал.

## Функционал

- При команде `/start` бот отправляет уведомление в указанный канал со ссылкой на пользователя
- Показывает три кнопки с формами регистрации:
  - 🚛 Водитель грузового авто
  - 🚗 Курьер на авто
  - 🚶 Пеший курьер

## Установка

1. Установите Python 3.10 или выше

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
copy .env.example .env
```

4. Заполните `.env` файл:
   - `BOT_TOKEN` - токен бота от @BotFather
   - `NOTIFICATION_CHANNEL_ID` - ID канала для уведомлений

### Как получить токен бота:
1. Напишите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

### Как получить ID канала:
1. Создайте канал (или используйте существующий)
2. Добавьте бота в канал как администратора с правом отправки сообщений
3. Отправьте любое сообщение в канал
4. Перейдите по ссылке: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
5. Найдите `chat.id` в ответе (будет выглядеть как `-1001234567890`)

## Запуск

```bash
python bot.py
```

## Структура проекта

```
.
├── bot.py              # Основной файл бота
├── config.py           # Конфигурация
├── .env                # Переменные окружения (создать вручную)
├── .env.example        # Пример файла .env
├── requirements.txt    # Зависимости
└── README.md          # Документация
```

## Примечания

- Бот работает в режиме long polling
- Все уведомления отправляются с HTML разметкой
- Логи выводятся в консоль

