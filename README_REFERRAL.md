# Реферальная система и админ-панель

## Новые возможности

### Реферальная система
- Пользователи могут приглашать друзей по реферальной ссылке
- Бонусы: 1000 руб пригласившему, 500 руб приглашённому
- Условия: 45 заказов для легкового тарифа, 30 для грузового
- Отслеживание приглашённых пользователей в профиле

### Функционал бота
- **👤 Профиль** — просмотр своей статистики и приглашённых
- **👥 Пригласить друзей** — получение реферальной ссылки
- **⚙️ Админ-панель** — доступна только администраторам

### Админ-панель
- Просмотр всех пользователей
- Информация о рефералах
- Статистика

## База данных

Автоматически создаётся файл `bot.db` с таблицами:
- `users` — пользователи
- `referrals` — связи между рефералами
- `orders_log` — лог заказов

## Настройка администраторов

### Способ 1: Через скрипт

```bash
# Добавить администратора
python set_admin.py add USER_ID

# Убрать администратора
python set_admin.py remove USER_ID

# Показать всех администраторов
python set_admin.py list
```

### Способ 2: Напрямую через БД

```python
from database import Database

db = Database()
db.set_admin(123456789, True)  # USER_ID администратора
```

## Как узнать USER_ID

1. Отправьте боту /start
2. Посмотрите в канале уведомлений — там будет ID пользователя
3. Или используйте @userinfobot в Telegram

## Уведомления в канал

Теперь включают:
- Категорию регистрации
- Информацию о пользователе
- **Кто пригласил** (если регистрация по реферальной ссылке)
- Фотографии документов одним сообщением

## Изменения в работе бота

1. **Убраны промежуточные сообщения** при отправке фото
2. После регистрации показывается информация о реферальной программе
3. Главное меню с кнопками вместо команд
4. Админ-панель для управления

## Пример работы

1. Пользователь A регистрируется и получает реферальную ссылку
2. Отправляет ссылку пользователю B
3. Пользователь B регистрируется по ссылке
4. В канал приходит уведомление: "Приглашён пользователем A"
5. После 45 заказов (или 30 для грузового) — бонусы

## Обновление на сервере

```bash
cd /root/tgbotkworkAlexzandr83
git pull origin main
systemctl restart tg-bot-registration.service
```

## Управление заказами (будущее)

Для подсчёта заказов можно:
1. Интегрировать API Яндекс.Флит
2. Ручной ввод администратором
3. Экспорт из статистики парка

Команда для обновления заказов:
```python
from database import Database
db = Database()
db.update_orders_count(user_id=123456, orders_count=45)
```

